# ====================================
# 🐾 Petshop Environment Variables
# ====================================

# 🔗 MySQL Database Connection
MYSQL_HOST=                   # Hostname of your MySQL server (e.g., localhost, 127.0.0.1, or remote server address)
MYSQL_PORT=                   # Default MySQL port (usually 3306)
MYSQL_USER=                   # MySQL username with appropriate privileges
MYSQL_PASSWORD=               # MySQL password for the above user (leave blank in sample)
MYSQL_DATABASE=               # Name of the database used by the application

# 🌐 Frontend URL
FRONT_END_URL=                # Base URL of the frontend application (e.g., http://localhost:3000 or https://yourdomain.com) for cors settings.

# 🔐 JWT Configuration
JWT_SIGN_KEY=                 # Secret key used to sign JWT tokens (use a long, secure random string)

# ⚠️ Notes:
# - This file is a sample and should not contain real credentials.
# - Create a `.env` file with actual values for local or production use.
# - Use environment variable loaders like `dotenv` to apply these settings in your app.
